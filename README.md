# CodeChallenge2
Pokédex project part 2, using:

To run the app is necessary to install to dependences:

```
npm install
bower install
```

And run the command:

```
gulp default
```

Screen:

![screen](screen.png)